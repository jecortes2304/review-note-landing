---
interface Props {
  basePath?: string;
}
const { basePath = '' } = Astro.props;
---

<header class="sticky top-0 z-50 bg-white/80 backdrop-blur-md border-b border-slate-100">
  <div class="max-w-6xl mx-auto px-6 h-16 flex items-center justify-between">
    <a href="/" class="flex items-center gap-2 group">
      <img src="/favicon.svg" alt="Note Reviewer Logo" class="w-6 h-6 group-hover:animate-spin-slow transition-transform" />
      <span class="font-semibold text-slate-900 text-sm tracking-tight">Note Reviewer</span>
    </a>

    <nav class="hidden md:flex items-center gap-8 text-sm font-medium text-slate-600">
      <a href={`${basePath}#features`}     class="hover:text-brand-700 transition-colors">Features</a>
      <a href={`${basePath}#how-it-works`} class="hover:text-brand-700 transition-colors">How it Works</a>
      <a href={`${basePath}#privacy`}      class="hover:text-brand-700 transition-colors">Privacy</a>
      <a href={`${basePath}#contact`}      class="hover:text-brand-700 transition-colors">Contact</a>
    </nav>

    <a
      href="https://chromewebstore.google.com"
      target="_blank"
      rel="noopener"
      class="inline-flex items-center gap-2 bg-brand-700 hover:bg-brand-800 text-white text-sm font-semibold px-5 py-2.5 rounded-xl transition-colors shadow-sm"
    >
      Get Started
      <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
      </svg>
    </a>
  </div>
</header>
